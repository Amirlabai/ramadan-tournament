GEMINI.md
status.md
.env
exgemini.md


# IDE / editor (local Cursor / VS Code config)
.cursor/
.vscode/

# graphify (regenerate locally; cache is machine-specific)
graphify-out/cache/
graphify-out/.graphify_python
graphify-out/.graphify_root
graphify-out/.graphify_detect.json
graphify-out/.graphify_uncached.txt
graphify-out/.graphify_labels.json

# Dependencies
node_modules/
server/node_modules
client/node_modules
.venv/

# Build outputs
server/dist
client/dist
shared/dist
server/src/**/*.js

# Vector index (workspace-chroma skill)
.chroma/

# Preregistration JSON (PII — generate locally / on deploy via npm run parse:prereg)
data/preregistration/*.json

# Environment (secrets only — *.env.example stays tracked)
.env
server/.env
client/.env
!server/.env.example
!client/.env.example
generate_message - Copy.py
__pycache__/
