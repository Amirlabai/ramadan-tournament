GEMINI.md
status.md
.env
exgemini.md


# IDE / editor (local Cursor / VS Code config)
.cursor/
.vscode/

# graphify (regenerate locally; output is machine-specific)
graphify-out/

# Dependencies
node_modules/
server/node_modules
client/node_modules
.venv/

# Build outputs
server/dist
client/dist
shared/dist
tools/analytics-explorer/dist
server/src/**/*.js

# Local analytics dashboard outputs (scripts/analytics_dashboard.py)
artifacts/

# Vector index (workspace-chroma skill)
.chroma/

# Preregistration JSON (PII — generate locally / on deploy via npm run parse:prereg)
data/preregistration/*.json

# Agent inbox — CSV/review files may contain PII
.incoming/*.csv
.incoming/parsed-teams-review.md

# Environment (secrets only — *.env.example stays tracked)
.env
server/.env
client/.env
!server/.env.example
!client/.env.example
generate_message - Copy.py
__pycache__/
.incoming/desktop banner.psd
.incoming/mobile banner.psd

# Upload compress / sync stage files (never commit)
server/uploads/**/*.compressing
server/uploads/**/*.rt-compress-tmp
server/uploads/**/*.rt-compress-bak
server/uploads/**/*.rt-sync-tmp
