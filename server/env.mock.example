# Copy to env.mock or server/.env for local dev without Render Postgres.
# Or run: npm run dev:mock (loads env.mock when present)
MOCK_DEV_DATA=1
JWT_SECRET=mock-dev-jwt-secret
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin123
NODE_ENV=development
